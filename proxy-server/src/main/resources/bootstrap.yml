spring:
  application:
    name: proxy-server
  cloud:
    config:
      uri: ${config.uri:http://localhost:8888}
      fail-fast: true

---

spring:
  profiles: docker
  cloud:
    config:
      uri: ${config.uri:http://config-server:8888}
      fail-fast: true