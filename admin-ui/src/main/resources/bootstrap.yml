spring:
  application:
    name: admin-ui
  cloud:
    config:
      uri: ${config.uri:http://localhost:8888}
      fail-fast: true

---

spring:
  profiles: docker
  cloud:
    config:
      uri: ${config.uri:http://config-server:8888}
      fail-fast: true